package flare.model.users;

public class Student extends User {

	//Used for testing messaging system
	public Student(int user_id, String userName, String first_name, String last_name) {
		super(user_id, userName, first_name, last_name);
	}

}